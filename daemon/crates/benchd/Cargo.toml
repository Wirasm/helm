[package]
name = "benchd"
version.workspace = true
edition.workspace = true
description = "The bench daemon: owns the record, answers on the socket."

[dependencies]
bench-wire = { path = "../bench-wire" }
bench-session = { path = "../bench-session" }
bench-mail = { path = "../bench-mail" }
serde_json.workspace = true
time.workspace = true
