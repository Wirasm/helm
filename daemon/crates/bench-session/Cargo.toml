[package]
name = "bench-session"
version.workspace = true
edition.workspace = true
description = "The pty core: daemon-owned sessions hosting full interactive agent TUIs."

[dependencies]
libc = "0.2"
rustix = { version = "1.1.5", features = ["fs", "process", "pty", "termios"] }
