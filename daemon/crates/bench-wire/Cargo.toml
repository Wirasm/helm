[package]
name = "bench-wire"
version.workspace = true
edition.workspace = true
description = "Every bench wire type and shared resolution rule, spelled once."

[dependencies]
bench-doc = { path = "../bench-doc" }
serde.workspace = true
serde_json.workspace = true
